<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SimpleArchive.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleArchive"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SimpleArchive">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Command="{x:Bind vm.FileSelectCommand}">파일 선택</Button>
        <Button Grid.Row="1" Command="{x:Bind vm.DecompressFileCommand}">압축 풀기</Button>
    </Grid>
</Window>
