<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="SimpleArchive.Views.ProgressWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SimpleArchive.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ProgressWindow">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>
        <ProgressBar Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Width="600" Value="{x:Bind vm.CurrentValue, Mode=OneWay}" Maximum="{x:Bind vm.MaxCurrent, Mode=OneWay}" />
        <ProgressBar Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Width="600" Value="{x:Bind vm.ArchiveValue, Mode=OneWay}" Maximum="{x:Bind vm.MaxArchive, Mode=OneWay}" />
        <TextBlock Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Text="{x:Bind vm.CurrentPath,Mode=OneWay}" />
    </Grid>
</Window>
